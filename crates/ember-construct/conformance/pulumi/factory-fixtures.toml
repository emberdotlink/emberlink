# CLASSIFICATION: PUBLIC
# P24 Pulumi construct-factory fixture skeleton.

[[fixtures]]
name = "version-credentialless"
argv = ["version"]
expected_action_ref = "registry.ember.systems/ember-systems/ember-pulumi/pulumi.version@v1"
expected_disposition = "credentialless"
expected_credential_injection = false
expected_ambient_credentials_stripped = true
reason = "local version inspection must not inherit cloud credentials"

[[fixtures]]
name = "up-needs-payload-analysis"
argv = ["up", "--yes"]
expected_disposition = "payload_analysis_required"
expected_credential_injection = false
reason = "Pulumi program and stack config determine provider resources and authority"

[[fixtures]]
name = "stack-rm-needs-resolver"
argv = ["stack", "rm", "prod"]
expected_disposition = "resolver_required"
expected_credential_injection = false
reason = "stack name must be resolved to trusted backend/project evidence"

[[fixtures]]
name = "login-fails-closed"
argv = ["login", "s3://state-bucket"]
expected_disposition = "unsupported_fail_closed"
expected_credential_injection = false
reason = "backend login mutates credential/source posture"
