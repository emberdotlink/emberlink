# CLASSIFICATION: PUBLIC
# P24 Vercel construct-factory fixture skeleton.

[[fixtures]]
name = "version-credentialless"
argv = ["--version"]
expected_action_ref = "registry.ember.systems/ember-systems/ember-vercel/vercel.version@v1"
expected_disposition = "credentialless"
expected_credential_injection = false
expected_ambient_credentials_stripped = true
reason = "local CLI version inspection must not inherit Vercel credentials"

[[fixtures]]
name = "deploy-needs-resolver"
argv = ["deploy", "--prod"]
expected_disposition = "resolver_required"
expected_credential_injection = false
reason = "project, team, cwd, and linked scope must be resolved before mediation"

[[fixtures]]
name = "env-pull-needs-payload-analysis"
argv = ["env", "pull", ".env.local"]
expected_disposition = "payload_analysis_required"
expected_credential_injection = false
reason = "environment material is authority-bearing payload and storage evidence"

[[fixtures]]
name = "token-flag-fails-closed"
argv = ["--token", "vercel-token", "deploy", "--prod"]
expected_disposition = "unsupported_fail_closed"
expected_credential_injection = false
reason = "token flag selects provider credential material"

[[fixtures]]
name = "login-fails-closed"
argv = ["login"]
expected_disposition = "unsupported_fail_closed"
expected_credential_injection = false
reason = "credential-bypass vector — Vercel auth must be broker-mediated"

[[fixtures]]
name = "whoami-credentialless"
argv = ["whoami"]
expected_action_ref = "registry.ember.systems/ember-systems/ember-vercel/vercel.whoami@v1"
expected_disposition = "credentialless"
expected_credential_injection = false
expected_ambient_credentials_stripped = true
reason = "read-only identity check must not inherit Vercel credentials"
