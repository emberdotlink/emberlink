[package]
name = "core-metrics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Prometheus-instrumentation primitives for Emberlink (per ADR 118)."

[dependencies]
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
