[package]
name = "core-grants-toml"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = ["std"]
std = []
schema = ["dep:schemars"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml.workspace = true
schemars = { version = "1", optional = true }

[lints]
workspace = true
